# elite-account-deletion

One static page (`index.html`), nothing else. No build step, no framework.
Content changes originate in the private app repo
(`Elite app/docs/release/account-deletion-page.html`) and are copied here —
never edited here first. Deploys to Vercel.
